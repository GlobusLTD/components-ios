platform :ios, '7.0'

target 'Globus-ObjC' do
	platform :ios, '7.0'

	pod 'Globus', :path => '../'

	pod 'Fabric'
	pod 'Crashlytics'
end

target 'Globus-Swift' do
	platform :ios, '8.0'
	
	use_frameworks!

	pod 'Globus', :path => '../'
	pod 'GlobusSwifty', :path => '../'

	pod 'Fabric'
	pod 'Crashlytics'
end
